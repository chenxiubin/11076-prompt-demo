services:
  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    restart: unless-stopped
    expose:
      - "8000"

  web:
    build:
      context: .
    depends_on:
      - backend
    restart: unless-stopped
    ports:
      - "80:80"
